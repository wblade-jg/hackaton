# Connection string for MySQL Database
ConnectionStrings__DefaultConnection=Server=localhost;Database=hackaton;User=root;Password=;

# Allowed origins for CORS (comma-separated)
Cors__AllowedOrigins=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Directory where CSV files to process are located
FileScanner__InputDirectory=/input
